name: healthfuture_new
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.2.2  # or latest stable version


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1 # <- use this older stable version

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon234.jpg"

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon234.jpg